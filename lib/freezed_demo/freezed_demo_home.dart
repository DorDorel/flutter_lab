import 'package:flutter/material.dart';

class LetsTryDataClassWithFreezed extends StatelessWidget {
  const LetsTryDataClassWithFreezed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Data Class With Freezed"),
      ),
    );
  }
}
